# Derma-Maze V6 Patch

Apply this patch only over the latest V5 full-site release.

- Copy all files and folders from this archive to the repository root.
- Allow replacement of existing files.
- Do not delete the unchanged assets/data folders.
- Keep ordering disabled until a real WhatsApp number is entered in `js/core/site-config.js`.

For the safest deployment, use the V6 full-site archive instead.
